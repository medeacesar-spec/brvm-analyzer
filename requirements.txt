# Core framework (>= 1.42 requis pour st.login / OAuth natif)
streamlit>=1.42,<1.50

# Authentification OAuth (Google) — requis par st.login()
Authlib>=1.3.2,<1.4

# Data
# NB : pandas < 2.3 et pyarrow < 20 pour éviter les SIGSEGV rapportés
# sur Streamlit Cloud avec numpy 2.5.x + pyarrow récent.
#
# numpy 2.1-2.2 (26/09/2026), et non plus < 2.0 : numpy 1.26 n'a AUCUNE roue
# pour Python 3.13 (celui de l'instance). Chaque redémarrage le COMPILAIT
# depuis les sources — plus de dix minutes. Le plafond < 2.0 datait du
# « Test 1 » du 15/07 (#11), pose le même jour que le passage à Python 3.13
# (#9), qui a réglé le segfault de 3.14 ; son utilité n'a jamais été
# établie. 2.1 et 2.2 ont leur roue cp313 et sont supportées par pandas 2.2
# et pyarrow 19. Si le SIGSEGV revenait : revenir ici, et passer plutôt
# l'instance en Python 3.12, où numpy 1.26 a sa roue.
pandas>=2.0,<2.3
numpy>=2.1,<2.3
pyarrow>=15,<20
openpyxl>=3.1

# Viz
plotly>=5.18

# HTTP et scraping
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9

# Analyse technique
ta>=0.11

# PDF extraction
pdfplumber>=0.11
PyMuPDF>=1.24

# OCR léger (import portefeuille depuis screenshot)
# Pont vers le binaire `tesseract`. INDISPENSABLE aux ateliers GitHub, qui
# lisent les bilans publiés en image et installent ce fichier ; ils posent le
# binaire eux-mêmes.
#
# L'instance Streamlit, elle, n'a pas le binaire : son paquet système passe par
# un apt-get que le constructeur fait échouer (dépôts Debian bullseye et trixie
# mélangés, fichier InRelease périmé). L'application ne s'en sert donc plus —
# l'import de portefeuille par capture d'écran a été retiré plutôt que d'offrir
# une fonction qui échoue à l'usage.
pytesseract>=0.3.10
Pillow>=10.0

# Base de données Postgres (Supabase)
psycopg[binary]>=3.2

# Stockage Supabase (upload PDFs)
supabase>=2.8

# TOML parsing (Python 3.9/3.10 compat — 3.11+ a tomllib natif)
tomli>=2.0;python_version<"3.11"
