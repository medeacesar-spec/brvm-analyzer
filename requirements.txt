# Core framework (>= 1.42 requis pour st.login / OAuth natif)
streamlit>=1.42

# Authentification OAuth (Google) — requis par st.login()
Authlib>=1.3.2

# Data
pandas>=2.0
numpy>=1.24
openpyxl>=3.1

# Viz
plotly>=5.18

# HTTP et scraping
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9

# Analyse technique
ta>=0.11

# PDF extraction
pdfplumber>=0.11
PyMuPDF>=1.24

# OCR léger (import portefeuille depuis screenshot)
# Nécessite aussi tesseract-ocr au niveau OS (cf packages.txt)
pytesseract>=0.3.10
Pillow>=10.0

# Base de données Postgres (Supabase)
psycopg[binary]>=3.2

# Stockage Supabase (upload PDFs)
supabase>=2.8

# TOML parsing (Python 3.9/3.10 compat — 3.11+ a tomllib natif)
tomli>=2.0;python_version<"3.11"
