# Core framework (>= 1.42 requis pour st.login / OAuth natif)
streamlit>=1.42,<1.50

# Authentification OAuth (Google) — requis par st.login()
Authlib>=1.3.2,<1.4

# Data
# NB : numpy < 2.0 et pandas < 2.3 pour éviter les SIGSEGV rapportés
# sur Streamlit Cloud avec numpy 2.5.x + pyarrow récent.
pandas>=2.0,<2.3
numpy>=1.24,<2.0
pyarrow>=15,<20
openpyxl>=3.1

# Viz
plotly>=5.18

# HTTP et scraping
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9

# Analyse technique
ta>=0.11

# PDF extraction
pdfplumber>=0.11
PyMuPDF>=1.24

# OCR léger (import portefeuille depuis screenshot)
# Pont vers le binaire `tesseract`, qui n'est PAS installé sur l'instance
# Streamlit : le constructeur mélange des dépôts Debian bullseye et trixie,
# et un fichier InRelease périmé y fait échouer tout apt-get — donc tout
# déploiement portant un packages.txt.
#
# Conséquence assumée : l'import d'un portefeuille par capture d'écran est
# indisponible dans l'application, et le dit. La lecture optique des PDF
# n'est pas touchée — les ateliers GitHub installent tesseract eux-mêmes.
pytesseract>=0.3.10
Pillow>=10.0

# Base de données Postgres (Supabase)
psycopg[binary]>=3.2

# Stockage Supabase (upload PDFs)
supabase>=2.8

# TOML parsing (Python 3.9/3.10 compat — 3.11+ a tomllib natif)
tomli>=2.0;python_version<"3.11"
